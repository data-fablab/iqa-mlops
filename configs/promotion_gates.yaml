feature_ae:
  recall_defect_min: 1.0
  false_negative_total_max: 0
  image_ap_max_regression: 0.02
  roi_fail_rate_max: 0.10
  latency_p95_ms_max: 1000

defect_coverage:
  # Gate de recevabilité : couverture minimale de défauts par source_class (IQA1_KEN02)
  min_coverage: 0.95

roi:
  # ROI segmenter figé — aucun entraînement ou remplacement sans nouvelle décision (IQA1_KEN02)
  model_version: roi_segmenter_v001_fixed
  frozen: true
